# React MySQL Form
A simple React form connected to a Node.js + MySQL backend.

## How to run
1. Clone the repo
2. Run `npm install`
3. Start backend with `node server.js`
4. Start frontend with `npm start`
